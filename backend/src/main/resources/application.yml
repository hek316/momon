spring:
  application:
    name: momon-backend

  # Disable Spring AI AutoConfiguration in Mock mode (default)
  autoconfigure:
    exclude:
      - org.springframework.ai.autoconfigure.openai.OpenAiAutoConfiguration

  # Virtual Threads 활성화 (Java 21)
  threads:
    virtual:
      enabled: true

  # Database (H2 for local development)
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  h2:
    console:
      enabled: true
      path: /h2-console

# AI Mock Mode Configuration
ai:
  mock:
    enabled: true  # Set to false in production for real AI calls

# AWS S3 설정 (Default profile)
aws:
  s3:
    bucket-name: ${AWS_S3_BUCKET_NAME:momon-images}
    region: ap-northeast-2
  credentials:
    access-key: ${AWS_ACCESS_KEY_ID:}
    secret-key: ${AWS_SECRET_ACCESS_KEY:}

---
# Docker profile (PostgreSQL)
spring:
  config:
    activate:
      on-profile: docker

  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://postgres:5432/momon}
    driver-class-name: org.postgresql.Driver
    username: ${SPRING_DATASOURCE_USERNAME:momon}
    password: ${SPRING_DATASOURCE_PASSWORD:momon123}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  h2:
    console:
      enabled: false

  # AI 설정 (Cost-optimized for production)
  ai:
    openai:
      api-key: ${OPENAI_API_KEY:your-api-key-here}
      chat:
        options:
          model: gpt-4o-mini  # 90% cheaper than gpt-4o
          temperature: 0.7
      image:
        options:
          model: dall-e-2  # 80% cheaper than dall-e-3
          size: 1024x1024
          quality: standard

# AWS S3 설정
aws:
  s3:
    bucket-name: ${AWS_S3_BUCKET_NAME:momon-images}
    region: ap-northeast-2
  credentials:
    access-key: ${AWS_ACCESS_KEY_ID:}
    secret-key: ${AWS_SECRET_ACCESS_KEY:}

# 서버 설정
server:
  port: 8080

# CORS 설정 (WebConfig 클래스에서 처리)
